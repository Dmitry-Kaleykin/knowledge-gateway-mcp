{
  "name": "knowledge-gateway-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "A policy-aware MCP gateway for retrieving and loading Pi skills through Scribery.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "knowledge-gateway-mcp": "./dist/cli.js"
  },
  "scripts": {
    "build": "node scripts/clean-dist.mjs && tsc --project tsconfig.build.json && node scripts/mark-executable.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "yaml": "^2.8.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
